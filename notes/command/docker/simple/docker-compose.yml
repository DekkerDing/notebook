version: '3.8'

services:
  redis-6379:
    image: redis:latest
    container_name: redis-6379
    ports:
      - "6379:6379"
      - "16379:16379" # cluster bus port
    volumes:
      - ./data/redis6379:/data
      - ./config/redis.conf:/usr/local/etc/redis/redis.conf
    command: ["redis-server", "/usr/local/etc/redis/redis.conf"]
    restart: unless-stopped
    networks:
      redis-cluster-net:
        ipv4_address: 10.101.11.10

  redis-6380:
    image: redis:latest
    container_name: redis-6380
    ports:
      - "6380:6379"
      - "16380:16380" # cluster bus port
    volumes:
      - ./data/redis6380:/data
      - ./config/redis.conf:/usr/local/etc/redis/redis.conf
    command: ["redis-server", "/usr/local/etc/redis/redis.conf"]
    restart: unless-stopped
    networks:
      redis-cluster-net:
        ipv4_address: 10.101.11.11

  redis-6381:
    image: redis:latest
    container_name: redis-6381
    ports:
      - "6381:6379"
      - "16381:16381" # cluster bus port
    volumes:
      - ./data/redis6381:/data
      - ./config/redis.conf:/usr/local/etc/redis/redis.conf
    command: ["redis-server", "/usr/local/etc/redis/redis.conf"]
    restart: unless-stopped
    networks:
      redis-cluster-net:
        ipv4_address: 10.101.11.12

networks:
  redis-cluster-net:
    external: true